enum PlaybackStatus {
  play,
  pause,
}

enum FilterBottomSheetStatus {
  enable,
  disable,
}

enum FilterEnabledType {
  text,
  price,
  duration,
  all,
}

enum OrderBy {
  name,
  duration,
  created,
}

enum TypeNotification {
  payment,
  simple,
}

enum LiqPayResponseStatus {
  success,
  wait,
  error,
  redirect,
}

enum NavigationStateStatus {
  initial,
  menu,
  tab,
}

enum WatchStatus {
  notViewed,
  inProgress,
  viewed,
}
